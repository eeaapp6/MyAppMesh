# Python/HTTP Extension Contract

The current release exposes the Python registration and HTTP/Python driver capabilities that are actually provided by FastCAE and APPMesh wrappers. Exact operations, request fields, permissions, timeout behavior and error representation follow the concrete registered wrapper. Stable request/response schemas, version negotiation and public error-code compatibility are deferred to a future interface version.
